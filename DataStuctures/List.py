letters = ["a", "b", "c"]
matrix = [[0, 1], [2, 4]]  # A 2 dimensional list
zeros = [0] * 5  # we can repeat the items in a list
combined = zeros + letters
print(combined)  # concatinating list
numbers = list(range(20))  # Creates a list from 0-20
chars = list("Hello World")  # will give each character their own index
print(len(chars))
